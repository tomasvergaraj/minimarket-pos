# Set to 'false' for production (real API calls)
# Set to 'true' for development with mock data
VITE_USE_MOCKS=false

# Optional: hard-code the API server URL
# If not set, the URL is read from localStorage (configurable in the admin panel)
# VITE_API_URL=http://localhost:8000
